import { NextPage } from "next";
import NpcMovement from "../../src/components/npc-movement/NpcMovement";

const NpcMovementPage: NextPage = () => {
    return <NpcMovement />;
};

export default NpcMovementPage;
